apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kokoro-fastapi.fullname" . }}-kokoro-tts
  labels:
    {{- include "kokoro-fastapi.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.kokoroTTS.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "kokoro-fastapi.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "kokoro-fastapi.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.kokoroTTS.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "kokoro-fastapi.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      initContainers: []
      containers:
        - name: kokoro-tts
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.kokoroTTS.repository }}:{{ .Values.kokoroTTS.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.kokoroTTS.pullPolicy }}
          {{- if .Values.kokoroTTS.env }}
          env:
            {{- toYaml .Values.kokoroTTS.env | nindent 12 }}
          {{- end }}
          ports:
            - name: kokoro-tts-http
              containerPort: {{ .Values.kokoroTTS.port | default 8880 }}
              protocol: TCP
          {{- if .Values.kokoroTTS.livenessProbe }}
          livenessProbe:
            {{- toYaml .Values.kokoroTTS.livenessProbe | nindent 12 }}
          {{- end }}
          {{- if .Values.kokoroTTS.readinessProbe }}
          readinessProbe:
            {{- toYaml .Values.kokoroTTS.readinessProbe | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.kokoroTTS.resources | nindent 12 }}
          volumeMounts: []
      volumes: []
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
